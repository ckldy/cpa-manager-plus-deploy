# B.AI plugin

Production-matched Go source with free-model labels, health probing and bounded fallback tests. Build: `go build -trimpath -buildmode=c-shared -o bai.so .`. No credentials are included.
